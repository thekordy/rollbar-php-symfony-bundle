imports:
    - { resource: config.yml }
    - { resource: parameters.yml }

rollbar:
  enable: true
  config:
    access_token: ~

monolog:
    handlers:
        rollbar:
            type: service
            id: Rollbar\Monolog\Handler\RollbarHandler